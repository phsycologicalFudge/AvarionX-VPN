import 'package:flutter/material.dart';
import '../dns/NetworkProtectionScreen.dart';

class FullVpnDnsTab extends StatelessWidget {
  const FullVpnDnsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const NetworkProtectionScreen();
  }
}